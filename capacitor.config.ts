import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'thirdweb-nft-drop-ionic',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
